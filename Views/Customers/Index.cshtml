@model IEnumerable<den_office.ViewModels.CustomersViewModel>
@{
    ViewBag.Title = "Index";
}
  
<a>afegrffg</a>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th scope="col"></th>

                            <th scope="col">Imię</th>

                            <th scope="col">Nazwisko</th>

                            <th scope="col">Numer telefonu</th>

                            <th scope="col">E-mail</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{var i = 1;                             }
                        @foreach (var user in Model)
                         
                        {
                        <tr>
                            <td>
                                @i
                            </td>
                            <td>
                                @user.FirstName

                            </td>
                            <td>
                                @user.Surname

                            </td>
                            <td>
                                @user.PhoneNumber
                            </td>
                            <td>
                                @user.Email
                            </td>
                        </tr>
                            i++;
                        }

                    </tbody>
                </table>

