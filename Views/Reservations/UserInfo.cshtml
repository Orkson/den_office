
@model IEnumerable<den_office.Models.Reservation>




<p> @ViewData["User"] </p>
<p> @ViewData["Email"]</p>

<p> @ViewData["Service"]</p>
@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ServiceDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ServiceId)
        </td>
    </tr>
}